import { Router } from 'express';
const routes = Router();

// TODO: Add your new component routes here:

// import testRoutes from '@src/@speak-test/routes';
// routes.use(testRoutes);

export default routes;